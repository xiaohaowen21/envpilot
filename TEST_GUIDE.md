# EnvPilot 测试指南

## 快速测试步骤

### 1. 启动应用

```bash
cd D:\CODE\envpilot
npm run dev
```

应用将在开发模式下启动，包含热重载功能。

### 2. 测试界面改进

#### 浅色/深色主题
1. 打开应用后，查看默认的浅色主题
2. 点击侧边栏底部的语言切换按钮旁边的主题设置
3. 切换到深色主题，观察：
   - 背景渐变变化
   - 卡片透明度和颜色
   - 按钮样式变化
   - 阴影效果

#### 动画效果测试
1. **悬停动画**
   - 将鼠标悬停在侧边栏的工作区卡片上
   - 观察卡片上浮效果和顶部装饰条出现
   
2. **按钮动画**
   - 悬停在任何按钮上
   - 观察按钮上浮、阴影增强、光泽效果
   
3. **进度条动画**
   - 安装一个运行时（如 Node.js）
   - 观察下载进度条的渐变流动和闪烁效果
   
4. **卡片动画**
   - 在"软件"或"版本"页面悬停在卡片上
   - 观察卡片向右移动和阴影变化

#### 交互测试
1. **搜索框**
   - 在"软件"页面使用搜索框
   - 聚焦时观察边框颜色和上浮效果
   
2. **折叠面板**
   - 点击任何折叠面板
   - 观察展开动画

3. **确认对话框**
   - 尝试卸载一个软件
   - 观察对话框的弹出动画和背景模糊效果

### 3. 测试环境变量写入功能

#### 前置条件
- **重要**：需要以管理员权限运行（如果要修改系统级环境变量）
- 建议先创建环境变量备份

#### 测试步骤

##### 3.1 安装运行时测试
1. 进入"版本管理"工作区
2. 选择一个运行时（如 Node.js）
3. 点击"安装"按钮，选择一个版本
4. 等待安装完成
5. **验证环境变量**：
   ```bash
   # 打开新的命令提示符窗口
   echo %NODE_HOME%
   # 应该显示类似：D:\EnvPilot\links\nodejs
   
   node --version
   # 应该显示安装的版本号
   ```

##### 3.2 切换运行时版本测试
1. 安装同一运行时的另一个版本
2. 点击"切换"按钮切换到新版本
3. **验证环境变量更新**：
   ```bash
   # 打开新的命令提示符
   node --version
   # 应该显示新版本号
   ```

##### 3.3 环境变量清理测试
1. 进入"环境变量"工作区
2. 点击"预览清理"按钮
3. 查看将要清理的项目（重复项、无效路径等）
4. 点击"自动清理"按钮
5. **验证清理结果**：
   ```bash
   # 打开新的命令提示符
   echo %PATH%
   # 检查是否移除了重复和无效的路径
   ```

##### 3.4 备份和恢复测试
1. 在页头点击"立即备份"按钮
2. 进入"设置"工作区
3. 在"备份管理"部分找到刚创建的备份
4. 点击"恢复"按钮测试备份恢复功能
5. 验证环境变量是否恢复到备份时的状态

### 4. 常见问题排查

#### 问题 1：环境变量未生效
**症状**：安装运行时后，命令行中找不到命令

**解决方案**：
1. 确认已经打开了**新的**命令提示符窗口
2. 检查"设置 > 环境诊断"查看详细状态
3. 查看操作日志中是否有错误信息
4. 手动检查注册表：
   ```
   HKEY_CURRENT_USER\Environment
   ```

#### 问题 2：需要管理员权限
**症状**：修改系统级环境变量失败

**解决方案**：
1. 右键点击应用，选择"以管理员身份运行"
2. 或者只修改用户级环境变量（不需要管理员权限）

#### 问题 3：动画效果不流畅
**症状**：界面动画卡顿

**解决方案**：
1. 确保使用的是现代浏览器内核（Electron 使用 Chromium）
2. 检查硬件加速是否开启
3. 降低系统动画效果设置

### 5. 性能测试

#### 测试项目
1. **启动时间**：应用从启动到显示主界面的时间
2. **环境变量扫描**：扫描所有环境变量的时间
3. **运行时安装**：下载和安装运行时的时间
4. **界面响应**：点击按钮后的响应延迟

#### 预期结果
- 启动时间：< 3 秒
- 环境变量扫描：< 1 秒
- 界面响应：< 100ms
- 动画流畅度：60 FPS

### 6. 自动化测试脚本

创建测试脚本验证环境变量写入：

```powershell
# test-env-write.ps1
Write-Host "测试环境变量写入功能..." -ForegroundColor Green

# 记录原始 PATH
$originalPath = [Environment]::GetEnvironmentVariable('Path', 'User')

# 添加测试路径
$testPath = "C:\EnvPilot_Test_$(Get-Random)"
$newPath = "$originalPath;$testPath"
[Environment]::SetEnvironmentVariable('Path', $newPath, 'User')

# 验证写入
$verifyPath = [Environment]::GetEnvironmentVariable('Path', 'User')
if ($verifyPath -eq $newPath) {
    Write-Host "✓ 环境变量写入成功" -ForegroundColor Green
} else {
    Write-Host "✗ 环境变量写入失败" -ForegroundColor Red
}

# 恢复原始值
[Environment]::SetEnvironmentVariable('Path', $originalPath, 'User')
Write-Host "✓ 已恢复原始环境变量" -ForegroundColor Green
```

运行测试：
```bash
powershell -ExecutionPolicy Bypass -File test-env-write.ps1
```

## 回归测试清单

在发布前确保以下功能正常：

- [ ] 应用启动和退出
- [ ] 主题切换（浅色/深色）
- [ ] 语言切换（中文/英文）
- [ ] 环境变量备份
- [ ] 环境变量恢复
- [ ] 环境变量清理
- [ ] 运行时安装
- [ ] 运行时切换
- [ ] 运行时卸载
- [ ] 系统工具安装/卸载
- [ ] 诊断功能
- [ ] 配置导入/导出
- [ ] 所有动画效果
- [ ] 响应式布局

## 测试报告模板

```
测试日期：____________________
测试人员：____________________
应用版本：0.1.0

### 界面测试
- [ ] 浅色主题显示正常
- [ ] 深色主题显示正常
- [ ] 所有动画流畅
- [ ] 悬停效果正常
- [ ] 响应式布局正常

### 功能测试
- [ ] 环境变量写入成功
- [ ] 环境变量读取正确
- [ ] 运行时安装成功
- [ ] 运行时切换成功
- [ ] 环境变量清理成功

### 性能测试
- 启动时间：________ 秒
- 环境变量扫描：________ 秒
- 界面响应延迟：________ ms

### 问题记录
1. ________________________________
2. ________________________________
3. ________________________________

### 总体评价
□ 通过  □ 需要修复  □ 严重问题
```

## 联系方式

如有问题请查看：
- GitHub Issues
- 项目文档
- IMPROVEMENTS.md
